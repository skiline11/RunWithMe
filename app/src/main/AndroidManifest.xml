<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.skiline11.pierwszaaplikacja">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FriendsRank" />
        <activity android:name=".PlanNewRunning" />
        <activity android:name=".AddNewResult" />
        <activity android:name=".AddNewFriends" />
        <activity android:name=".MainMenu" />
        <activity android:name=".FriendRankList" />
        <activity android:name=".RunRankList" />
        <activity android:name=".YourResults" />
        <activity android:name=".ChooseDistance"></activity>
    </application>

</manifest>